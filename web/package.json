{
  "name": "diy-battery-bms-web-flasher",
  "version": "0.1.0",
  "private": true,
  "description": "Browser flasher for DIY Battery BMS - builds into ../site",
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.js --bundle --format=esm --target=es2022 --minify --outfile=../site/app.js",
    "dev": "esbuild src/app.js --bundle --format=esm --target=es2022 --outfile=../site/app.js --watch",
    "restore": "node scripts/restore-published.mjs",
    "manifests": "node scripts/build-manifests.mjs",
    "release": "node scripts/publish-release.mjs",
    "pages": "npx --yes wrangler@latest pages deploy ../site --project-name diy-power-pilot-uk --branch main --commit-dirty=true",
    "deploy": "npm run build && npm run restore && npm run manifests && npm run release && npm run pages",
    "deploy:site": "npm run build && npm run pages"
  },
  "dependencies": {
    "esptool-js": "^0.5.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
